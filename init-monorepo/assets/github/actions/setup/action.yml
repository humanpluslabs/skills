name: Setup
description: pnpm, Node.js, and install dependencies

runs:
  using: composite
  steps:
    - uses: pnpm/action-setup@v5
    - uses: actions/setup-node@v6
      with:
        node-version-file: package.json
        cache: pnpm
    - run: pnpm install
      shell: bash
