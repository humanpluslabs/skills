# <PROJECT_NAME>

A TypeScript monorepo managed with [pnpm workspaces](https://pnpm.io/workspaces).

## Requirements

- Node.js `>=<NODE_LTS_VERSION>`
- pnpm (version pinned via `packageManager` in `package.json`)

## Getting started

```sh
pnpm install
```

## Project layout

Workspace packages will live under top-level directories declared in `pnpm-workspace.yaml`.
